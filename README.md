# netlify-tidb-chat

示例项目：使用 Netlify Functions + TiDB 的简单聊天室前端与后端示例。

按仓库结构包含：
- Netlify functions: `messages.js`, `send-message.js`, `db-connect.js`。
- React + Vite 前端。

查看并填写 `.env`（参考 `.env.example`）后，本地开发：

```bash
npm install
npm run dev
```
